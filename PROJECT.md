# office-vision（办公视觉 AI 多端系统 / 吸烟检测 YOLO）

- **进度**: 85%
- **当前阶段**: 吸烟检测已按「烟在手/嘴才算」位置规则重构上线（VM 115），标注已集成 monitor 页，Windows 正从好模型 CPU 微调
- **更新**: 2026-08-28

## 问题 / 阻塞

- 吸烟识别精确度不足，疑似标注数据量/多样性不够（香烟为极小目标、webcam 下仅几十像素；mAP50 97.5% 为验证集成绩，真实办公场景泛化有风险）
- Windows 训练机 CUDA torch（1.8GiB）大文件经 mihomo 代理仍被掐断（0 字节），GPU 重训未打通
- 训练数据约 100–200 张，smoking 分类曾因「全抬头/全低头」捷径学习误判
- Windows 训练进行中（80 epoch / CPU 约 1h，mAP50≈0.95–0.98），结束后需取 `best.pt` 部署回 VM 115；标注仍偏少（08-28 新增 smoking 50 + normal 12）（08-28 新增）

## 下一步

- Windows 训练结束后取新 `best.pt` → 覆盖 `weights/cigarette-best.pt` → 同步回 VM 115（agent 换权重重启）→ 验证抽烟检测
- 用户实测 `http://192.168.9.214:3000/monitor`（烟在手/嘴触发、放桌上不算、拿笔/摸耳不误报、小目标检出），反馈后按需调参
- 继续采难例（角度/光照/遮挡/小目标 + 难负样本）；打通 Windows GPU 训练
- 待定：VM 115 是否改成 `git pull` 自动跟随 GitHub 更新

